export enum WorkflowStatus {
    PENDING = 'pending',
    APPROVE = 'approved',
    DENY = 'denied',
    QUEUE = 'queued',
    IN_PROGRESS = 'in_progress',
    COMPLETE = 'completed',
    FAILURE = 'failure',
    SUCCESS = 'success',
}